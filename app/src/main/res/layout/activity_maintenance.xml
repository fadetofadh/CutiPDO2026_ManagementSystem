<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp">

    <ImageView
        android:id="@+id/ivMaintenanceIcon"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:contentDescription="Maintenance Icon"
        android:src="@drawable/ic_launcher_foreground"
        app:tint="@color/black" />

    <TextView
        android:id="@+id/tvMaintenanceTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="@string/maintenance_alert_title"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="22sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tvMaintenanceMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="Sistem sedang dalam perbaikan berkala untuk meningkatkan layanan. Kami akan segera kembali."
        android:textAlignment="center"
        android:textColor="#666666"
        android:textSize="16sp" />

    <Button
        android:id="@+id/btnExitMaintenance"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="48dp"
        android:padding="12dp"
        android:text="@string/maintenance_exit_btn"
        android:textAllCaps="true"
        android:textSize="16sp"
        android:backgroundTint="#2D3748"
        android:textColor="@color/white" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="@string/placeholder_text"
        android:textColor="#999999"
        android:textSize="12sp" />

</LinearLayout>